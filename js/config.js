globalThis.STB = globalThis.STB || {};
// Both values must be filled in with real ones before the app can run:
// APPS_SCRIPT_URL from Task 1 Step 7 (the /exec deployment URL), and
// GOOGLE_CLIENT_ID from Task 1 Step 2 (the OAuth client id).
STB.config = {
  APPS_SCRIPT_URL: '<PASTE THE /exec URL FROM TASK 1 STEP 7>',
  GOOGLE_CLIENT_ID: '<PASTE THE CLIENT ID FROM TASK 1 STEP 2>'
};
